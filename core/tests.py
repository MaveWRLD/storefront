# Tests removed with storefront/container_metrics.py during the
# monitoring-code cleanup (Sentry/OTel/Prometheus removal) — no monitoring
# code left in `core` to test. Add tests here as `core` grows its own logic.
